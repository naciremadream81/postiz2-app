# =============================================================================
# POSTIZ DEVELOPMENT ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains example environment variables for local development.
# Copy this file to .env and modify values as needed.
# 
# To use: cp docker.env.example .env
# 
# SECURITY WARNING: Never commit .env files to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Configuration
# -----------------------------------------------------------------------------
# Database credentials for PostgreSQL
# Change these values for your local development environment
POSTGRES_USER=postiz-local
POSTGRES_PASSWORD=postiz-local-pwd
POSTGRES_DB=postiz-db-local
POSTGRES_PORT=5432

# Database connection string (if using URI format)
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postiz-postgres:5432/${POSTGRES_DB}

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
# Redis password for authentication
# In production, use a strong, randomly generated password
REDIS_PASSWORD=dev-redis-password
REDIS_PORT=6379

# Redis connection string (if using URI format)
# REDIS_URL=redis://:${REDIS_PASSWORD}@postiz-redis:6379

# -----------------------------------------------------------------------------
# pgAdmin Configuration
# -----------------------------------------------------------------------------
# Web interface for PostgreSQL management
# Access at http://localhost:8081
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=8081

# -----------------------------------------------------------------------------
# RedisInsight Configuration
# -----------------------------------------------------------------------------
# Web interface for Redis management
# Access at http://localhost:5540
REDISINSIGHT_PORT=5540

# -----------------------------------------------------------------------------
# Cloudflare Tunnel Configuration
# -----------------------------------------------------------------------------
# Cloudflare Tunnel provides secure access to your application
# Public URL: https://postiz.swonger-armstrong.org
# 
# You need to:
# 1. Create a tunnel at https://one.dash.cloudflare.com/
# 2. Generate credentials and save to cloudflared-credentials.json
# 3. Update cloudflare-tunnel-config.yaml with your tunnel ID
# See CLOUDFLARE_TUNNEL_SETUP.md for detailed instructions
CLOUDFLARE_TUNNEL_ID=YOUR_TUNNEL_ID_HERE
CLOUDFLARE_TUNNEL_URL=https://postiz.swonger-armstrong.org

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Node environment (development, production, test)
NODE_ENV=development

# Application ports
FRONTEND_PORT=3000
BACKEND_PORT=3000
API_PORT=4200

# Application secrets
# Generate strong random strings for production
# You can use: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-change-in-production
SESSION_SECRET=your-session-secret-change-in-production
ENCRYPTION_KEY=your-encryption-key-change-in-production

# -----------------------------------------------------------------------------
# Docker Build Configuration
# -----------------------------------------------------------------------------
# Version tag for the application
NEXT_PUBLIC_VERSION=dev

# Node.js memory limit for builds (in MB)
NODE_MAX_MEMORY=4096

# PNPM version to use
PNPM_VERSION=10.6.1

# -----------------------------------------------------------------------------
# Optional: External Services
# -----------------------------------------------------------------------------
# Uncomment and configure if you're using external services

# AWS Configuration
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# S3_BUCKET=

# Email Service (SendGrid, AWS SES, etc.)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM_EMAIL=

# OAuth Providers
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# Monitoring & Analytics
# SENTRY_DSN=
# GOOGLE_ANALYTICS_ID=

# -----------------------------------------------------------------------------
# Development Tools
# -----------------------------------------------------------------------------
# Enable debug logging
DEBUG=false

# Enable hot reload
HOT_RELOAD=true

# Enable source maps
SOURCE_MAPS=true

